# Paper → Tailwind CSS v4

The authoritative guide is packaged with the skill: [Paper → Tailwind CSS v4](skills/paper-tailwind-tokens/references/integration.md).

Evidence and regressions live in [`tests/`](tests/README.md) — `npm test` for the
local lanes, plus two written protocols for what the local lanes cannot prove.
